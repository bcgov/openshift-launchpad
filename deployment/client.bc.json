{
  "apiVersion": "v1",
  "kind": "Template",
  "labels": {
    "app": "${APP_NAME}",
    "template": "${APP_NAME}-client"
  },
  "parameters": [
    {
      "name": "NAMESPACE",
      "required": true
    },
    {
      "name": "APP_NAME",
      "required": true
    },
    {
      "name": "REPO",
      "required": true
    },
    {
      "name": "BRANCH",
      "required": true
    },
    {
      "name": "API_URL",
      "required": true
    }
  ],
  "objects": [
    {
      "apiVersion": "v1",
      "kind": "ImageStream",
      "metadata": {
        "name": "${APP_NAME}-client",
        "namespace": "${NAMESPACE}"
      }
    },
    {
      "apiVersion": "v1",
      "kind": "BuildConfig",
      "metadata": {
        "name": "${APP_NAME}-client"
      },
      "spec": {
        "output": {
          "to": {
            "kind": "ImageStreamTag",
            "name": "${APP_NAME}-client:latest",
            "namespace": "${NAMESPACE}"
          }
        },
        "source": {
          "contextDir": "client",
          "git": {
            "uri": "${REPO}",
            "ref": "origin/${BRANCH}"
          },
          "type": "Git"
        },
        "strategy": {
          "type": "Docker",
          "dockerStrategy": {
            "env": [
              {
                "name": "API_URL",
                "value": "${API_URL}/api"
              }
            ]
          }
        },
        "triggers": [
          {
            "type": "ImageChange"
          },
          {
            "type": "ConfigChange"
          }
        ]
      }
    }
  ]
}
